@model ReportListVM

@{
    ViewData["Title"] = "report's list";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<form method="get">

    <div class="col-12 text-center">
        <h1 class="text-secondary"> Report's List</h1>
    </div>
    <br />
    <br />
    <div class="row container">
        <div class="col-11">
            <div class="row" style="padding-top:10px">
                <div class="col-6">
                    @Html.Editor("searchName", new { htmlAttributes = new { @class = "form-control", @placeholder = "Product name..." } })
                </div>
                <div class="col-6">
                    @Html.Editor("searchCategory", new { htmlAttributes = new { @class = "form-control", @placeholder = "Product category..." } })
                </div>
            </div>
        </div>
        <div class="col-1">
            <div class="row" style="padding-top:10px;">
                <button type="submit" name="submit" value="Search" class="btn btn-info form-control">
                    <i class="fas fa-search"></i>
                </button>
            </div>
        </div>
    </div>
    <br />
    <br />
    <div class="p-1">
        @if (Model.Reports.Count() > 0)
        {
            <table class="table">
                <thead class="thead-dark">
                    <tr>
                        <th>
                            Product Name
                        </th>
                        <th>
                            Category Name
                        </th>
                        <th>
                            Subcategory Name
                        </th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                @foreach (var item in Model.Reports)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(m => item.ProductName)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.CategoryName)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.SubCategoryName)
                        </td>
                        <td>
                            <partial name="_TableButtonPartial" model="item.Id" />
                        </td>
                    </tr>
                }
            </table>
            <div page-model="@Model.PagingInfo" page-classes-enabled="true" page-class="btn border"
                 page-class-normal="btn btn-light" page-class-selected="btn btn-info active"
                 class="btn-group float-right "></div>
            <br />
            <br />
        }
        else
        {
            <p>No reports exist...</p>
        }
    </div>
    <br />    
    <div class="col-6 offset-6 text-right">
        <partial name="_CreateButtonPartial" />
    </div>
</form>

